import { Typography } from "@mui/material";

import { Layout } from "../layouts";

export default function Histogram() {
  return (
    <Layout>
      <Typography variant="h2">Welcome to Histogram.</Typography>
    </Layout>
  );
}
